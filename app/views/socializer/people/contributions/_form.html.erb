<%= simple_form_for(contribution) do |f| %>
  <%= f.input :label, input_html: { autofocus: true } %>
  <%= f.input :url %>
  <%= f.input :current %>

  <%= f.button :submit, class: "btn-primary action-button" %>
<% end %>
