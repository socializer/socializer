<h3><%= @person.display_name %></h3>
<%= link_to "Send a message", @person, :class => 'action-button' %>
<p>In <%= @person.display_name %>'s circles (<%= @person.contacts.size %>):</p>
<ul>
<% @person.contacts.each do |contact| %>
<li><%= link_to contact.display_name, stream_path(:provider => :people, :id => contact.id) %>
<% end %>
</ul>
<p>Have <%= @person.display_name %> in circles (<%= @person.contact_of.size %>):</p>
<ul>
<% @person.contact_of.each do |person| %>
<li><%= link_to person.display_name, stream_path(:provider => :people, :id => person.id) %>
<% end %>
</ul>