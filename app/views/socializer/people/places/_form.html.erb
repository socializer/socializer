<%= simple_form_for([person, place]) do |f| %>
  <%= f.input :city_name, input_html: { autofocus: true } %>
  <%= f.input :current %>

  <%= f.button :submit, class: "btn-primary action-button" %>
<% end %>
