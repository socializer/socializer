<%= simple_form_for([person, profile]) do |f| %>
  <%= f.input :display_name, input_html: { autofocus: true } %>
  <%= f.input :url %>

  <%= f.button :submit, class: "btn-primary action-button" %>
<% end %>
