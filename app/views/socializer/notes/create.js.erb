if (window.location.href.indexOf('message') > 0) {
  window.location.href = '<%= stream_path %>';
} else {
  $('.activities').prepend('<%= escape_javascript(render @activity) %>');
  // TODO: Note - create.js.erb - no need to re-render the partial when using the resetNoteForm function.
  //        If re-rendering is preferred, some modifications should be made to resetNoteForm
  //        replace instance variable with local variable may change things
  // $('.new_note').replaceWith('<%= escape_javascript(render 'socializer/notes/form', note: @note, title: @title, current_id: @current_id) %>');
  resetNoteForm();
  addTimeAgoSupport();
  addTooltipSupport( $('.tooltip', '.activities') );
}
