<%= render 'menu' %>
<ul class="large-contact-list">
<% current_user.contacts.each do |contact| %>
<li class="draggable"><%= link_to contact.display_name, stream_path(:provider => :people, :id => contact.id), "data-object-id" => contact.id %>
<% end %>
</ul>
<%= render 'circles' %>